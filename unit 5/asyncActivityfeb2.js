// a situation where you can use while loops is when setting up a password becuase you can continuously tell a user no until the use puts the right number

// Function to find the highest grade in an array
function findHighestGrade(grades) {
    // Initialize highest grade to the first grade in the list
    var highestGrade = grades[0]

     // Call the function to find and display the highest grade
     console.log(findHighestGrade(grades));
    }
   { 
    // Start collecting grades
    collectGrades();
// Result
    return "The highest grade is: " + highestGrade;




}